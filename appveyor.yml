version: 1.0.{build}
configuration:
  - Debug
  - Release
image: Visual Studio 2017
publish_nuget: true
install:
- cmd: >-
    dotnet restore
build:
  verbosity: minimal
cache:
  - '%USERPROFILE%\.nuget\packages'
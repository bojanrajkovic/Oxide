version: 1.0.{build}
configuration:
  - Debug
  - Release
image: Visual Studio 2017
install:
- cmd: >-
    dotnet restore
build:
  verbosity: minimal
  publish_nuget: true
  publish_nuget_symbols: true
cache:
  - '%USERPROFILE%\.nuget\packages'